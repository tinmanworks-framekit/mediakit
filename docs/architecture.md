# MediaKit Architecture

## Components
1. source contract (`MediaSource`)
2. session lifecycle (`MediaSession`)
3. decoder extension point (`IDecoderPlugin`)

MediaKit is optional and can be linked only by applications that need media features.
